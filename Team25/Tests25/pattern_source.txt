procedure Alpha {
   a = ball;
   if (c < d) then {
       a = hello;
       if (ab < cd) then {
           print c;
       } else {
           d = a * e + 2;
           if (ab < cd) then {
               print c;
           } else {
               d = a * (e+2) / (5 * 6) + ((e * k) * (e));
           }
       }
       print a;
   } else {
       print = hello+2;
   }
   while (i > j) {
       k = l;
       while (n > i) {
           ball = l;
           while (ij > in) {
               ball = ball;
           }
       }
       print z;
   }
   print a;
   print b;
}
